===========================================================================

线性结构常见： 
数组，队列，链表，栈

顺序存储(存储元素    一定连续)：数组
链式存储(存储元素不一定连续)：链表


非线性结构：
二维数组，多维数组，广义表，树结构，图结构

===========================================================================

稀疏数组: 如果二维数组很多值默认是0等无意义值的话，就可以使用稀疏数组保存该数组